<?xml version="1.0" encoding="UTF-8"?>
<!--
  BFF (Backend for Frontend): la única puerta de entrada del frontend React.
  Valida el token y agrega en una sola respuesta lo que cada pantalla necesita.
  No tiene base de datos propia.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cl.duoc.duocconecta</groupId>
        <artifactId>duocconecta</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>bff-web</artifactId>
    <name>bff-web</name>
    <description>Backend for Frontend de DuocConecta</description>

    <dependencies>
        <!-- Misma validación de JWT que los microservicios: defensa en profundidad. -->
        <dependency>
            <groupId>cl.duoc.duocconecta</groupId>
            <artifactId>common-seguridad</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- En Spring Boot 4 el soporte de MockMvc en tests vive en su propio artefacto,
             ya no viene dentro de spring-boot-starter-test. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
